[package]
name = "igloo-cache"
version = "0.1.0"
edition = "2021"

[dependencies]
igloo-common = { path = "../common" }
tracing = "0.1"
tokio = { version = "1", features = ["full"] }
tonic = "0.12"
prost = "0.13"
prost-types = "0.13"
# arrow = { version = "35.0.0" } # Commented out due to persistent compilation issues with transitive dependencies

[dev-dependencies]
futures = "0.3"

[features]
default = []
